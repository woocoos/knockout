// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/woocoos/knockout/ent/org"
	"github.com/woocoos/knockout/ent/predicate"
	"github.com/woocoos/knockout/ent/userpasswordpolicy"
)

// UserPasswordPolicyQuery is the builder for querying UserPasswordPolicy entities.
type UserPasswordPolicyQuery struct {
	config
	ctx        *QueryContext
	order      []userpasswordpolicy.OrderOption
	inters     []Interceptor
	predicates []predicate.UserPasswordPolicy
	withOrg    *OrgQuery
	modifiers  []func(*sql.Selector)
	loadTotal  []func(context.Context, []*UserPasswordPolicy) error
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UserPasswordPolicyQuery builder.
func (uppq *UserPasswordPolicyQuery) Where(ps ...predicate.UserPasswordPolicy) *UserPasswordPolicyQuery {
	uppq.predicates = append(uppq.predicates, ps...)
	return uppq
}

// Limit the number of records to be returned by this query.
func (uppq *UserPasswordPolicyQuery) Limit(limit int) *UserPasswordPolicyQuery {
	uppq.ctx.Limit = &limit
	return uppq
}

// Offset to start from.
func (uppq *UserPasswordPolicyQuery) Offset(offset int) *UserPasswordPolicyQuery {
	uppq.ctx.Offset = &offset
	return uppq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (uppq *UserPasswordPolicyQuery) Unique(unique bool) *UserPasswordPolicyQuery {
	uppq.ctx.Unique = &unique
	return uppq
}

// Order specifies how the records should be ordered.
func (uppq *UserPasswordPolicyQuery) Order(o ...userpasswordpolicy.OrderOption) *UserPasswordPolicyQuery {
	uppq.order = append(uppq.order, o...)
	return uppq
}

// QueryOrg chains the current query on the "org" edge.
func (uppq *UserPasswordPolicyQuery) QueryOrg() *OrgQuery {
	query := (&OrgClient{config: uppq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := uppq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := uppq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(userpasswordpolicy.Table, userpasswordpolicy.FieldID, selector),
			sqlgraph.To(org.Table, org.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, userpasswordpolicy.OrgTable, userpasswordpolicy.OrgColumn),
		)
		fromU = sqlgraph.SetNeighbors(uppq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first UserPasswordPolicy entity from the query.
// Returns a *NotFoundError when no UserPasswordPolicy was found.
func (uppq *UserPasswordPolicyQuery) First(ctx context.Context) (*UserPasswordPolicy, error) {
	nodes, err := uppq.Limit(1).All(setContextOp(ctx, uppq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{userpasswordpolicy.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (uppq *UserPasswordPolicyQuery) FirstX(ctx context.Context) *UserPasswordPolicy {
	node, err := uppq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UserPasswordPolicy ID from the query.
// Returns a *NotFoundError when no UserPasswordPolicy ID was found.
func (uppq *UserPasswordPolicyQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = uppq.Limit(1).IDs(setContextOp(ctx, uppq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{userpasswordpolicy.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (uppq *UserPasswordPolicyQuery) FirstIDX(ctx context.Context) int {
	id, err := uppq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UserPasswordPolicy entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UserPasswordPolicy entity is found.
// Returns a *NotFoundError when no UserPasswordPolicy entities are found.
func (uppq *UserPasswordPolicyQuery) Only(ctx context.Context) (*UserPasswordPolicy, error) {
	nodes, err := uppq.Limit(2).All(setContextOp(ctx, uppq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{userpasswordpolicy.Label}
	default:
		return nil, &NotSingularError{userpasswordpolicy.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (uppq *UserPasswordPolicyQuery) OnlyX(ctx context.Context) *UserPasswordPolicy {
	node, err := uppq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UserPasswordPolicy ID in the query.
// Returns a *NotSingularError when more than one UserPasswordPolicy ID is found.
// Returns a *NotFoundError when no entities are found.
func (uppq *UserPasswordPolicyQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = uppq.Limit(2).IDs(setContextOp(ctx, uppq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{userpasswordpolicy.Label}
	default:
		err = &NotSingularError{userpasswordpolicy.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (uppq *UserPasswordPolicyQuery) OnlyIDX(ctx context.Context) int {
	id, err := uppq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UserPasswordPolicies.
func (uppq *UserPasswordPolicyQuery) All(ctx context.Context) ([]*UserPasswordPolicy, error) {
	ctx = setContextOp(ctx, uppq.ctx, ent.OpQueryAll)
	if err := uppq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UserPasswordPolicy, *UserPasswordPolicyQuery]()
	return withInterceptors[[]*UserPasswordPolicy](ctx, uppq, qr, uppq.inters)
}

// AllX is like All, but panics if an error occurs.
func (uppq *UserPasswordPolicyQuery) AllX(ctx context.Context) []*UserPasswordPolicy {
	nodes, err := uppq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UserPasswordPolicy IDs.
func (uppq *UserPasswordPolicyQuery) IDs(ctx context.Context) (ids []int, err error) {
	if uppq.ctx.Unique == nil && uppq.path != nil {
		uppq.Unique(true)
	}
	ctx = setContextOp(ctx, uppq.ctx, ent.OpQueryIDs)
	if err = uppq.Select(userpasswordpolicy.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (uppq *UserPasswordPolicyQuery) IDsX(ctx context.Context) []int {
	ids, err := uppq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (uppq *UserPasswordPolicyQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, uppq.ctx, ent.OpQueryCount)
	if err := uppq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, uppq, querierCount[*UserPasswordPolicyQuery](), uppq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (uppq *UserPasswordPolicyQuery) CountX(ctx context.Context) int {
	count, err := uppq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (uppq *UserPasswordPolicyQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, uppq.ctx, ent.OpQueryExist)
	switch _, err := uppq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (uppq *UserPasswordPolicyQuery) ExistX(ctx context.Context) bool {
	exist, err := uppq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UserPasswordPolicyQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (uppq *UserPasswordPolicyQuery) Clone() *UserPasswordPolicyQuery {
	if uppq == nil {
		return nil
	}
	return &UserPasswordPolicyQuery{
		config:     uppq.config,
		ctx:        uppq.ctx.Clone(),
		order:      append([]userpasswordpolicy.OrderOption{}, uppq.order...),
		inters:     append([]Interceptor{}, uppq.inters...),
		predicates: append([]predicate.UserPasswordPolicy{}, uppq.predicates...),
		withOrg:    uppq.withOrg.Clone(),
		// clone intermediate query.
		sql:  uppq.sql.Clone(),
		path: uppq.path,
	}
}

// WithOrg tells the query-builder to eager-load the nodes that are connected to
// the "org" edge. The optional arguments are used to configure the query builder of the edge.
func (uppq *UserPasswordPolicyQuery) WithOrg(opts ...func(*OrgQuery)) *UserPasswordPolicyQuery {
	query := (&OrgClient{config: uppq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	uppq.withOrg = query
	return uppq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedBy int `json:"created_by,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UserPasswordPolicy.Query().
//		GroupBy(userpasswordpolicy.FieldCreatedBy).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (uppq *UserPasswordPolicyQuery) GroupBy(field string, fields ...string) *UserPasswordPolicyGroupBy {
	uppq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UserPasswordPolicyGroupBy{build: uppq}
	grbuild.flds = &uppq.ctx.Fields
	grbuild.label = userpasswordpolicy.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedBy int `json:"created_by,omitempty"`
//	}
//
//	client.UserPasswordPolicy.Query().
//		Select(userpasswordpolicy.FieldCreatedBy).
//		Scan(ctx, &v)
func (uppq *UserPasswordPolicyQuery) Select(fields ...string) *UserPasswordPolicySelect {
	uppq.ctx.Fields = append(uppq.ctx.Fields, fields...)
	sbuild := &UserPasswordPolicySelect{UserPasswordPolicyQuery: uppq}
	sbuild.label = userpasswordpolicy.Label
	sbuild.flds, sbuild.scan = &uppq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UserPasswordPolicySelect configured with the given aggregations.
func (uppq *UserPasswordPolicyQuery) Aggregate(fns ...AggregateFunc) *UserPasswordPolicySelect {
	return uppq.Select().Aggregate(fns...)
}

func (uppq *UserPasswordPolicyQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range uppq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, uppq); err != nil {
				return err
			}
		}
	}
	for _, f := range uppq.ctx.Fields {
		if !userpasswordpolicy.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if uppq.path != nil {
		prev, err := uppq.path(ctx)
		if err != nil {
			return err
		}
		uppq.sql = prev
	}
	return nil
}

func (uppq *UserPasswordPolicyQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UserPasswordPolicy, error) {
	var (
		nodes       = []*UserPasswordPolicy{}
		_spec       = uppq.querySpec()
		loadedTypes = [1]bool{
			uppq.withOrg != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UserPasswordPolicy).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UserPasswordPolicy{config: uppq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(uppq.modifiers) > 0 {
		_spec.Modifiers = uppq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, uppq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := uppq.withOrg; query != nil {
		if err := uppq.loadOrg(ctx, query, nodes, nil,
			func(n *UserPasswordPolicy, e *Org) { n.Edges.Org = e }); err != nil {
			return nil, err
		}
	}
	for i := range uppq.loadTotal {
		if err := uppq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (uppq *UserPasswordPolicyQuery) loadOrg(ctx context.Context, query *OrgQuery, nodes []*UserPasswordPolicy, init func(*UserPasswordPolicy), assign func(*UserPasswordPolicy, *Org)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*UserPasswordPolicy)
	for i := range nodes {
		fk := nodes[i].TenantID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(org.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "tenant_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (uppq *UserPasswordPolicyQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := uppq.querySpec()
	if len(uppq.modifiers) > 0 {
		_spec.Modifiers = uppq.modifiers
	}
	_spec.Node.Columns = uppq.ctx.Fields
	if len(uppq.ctx.Fields) > 0 {
		_spec.Unique = uppq.ctx.Unique != nil && *uppq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, uppq.driver, _spec)
}

func (uppq *UserPasswordPolicyQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(userpasswordpolicy.Table, userpasswordpolicy.Columns, sqlgraph.NewFieldSpec(userpasswordpolicy.FieldID, field.TypeInt))
	_spec.From = uppq.sql
	if unique := uppq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if uppq.path != nil {
		_spec.Unique = true
	}
	if fields := uppq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, userpasswordpolicy.FieldID)
		for i := range fields {
			if fields[i] != userpasswordpolicy.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if uppq.withOrg != nil {
			_spec.Node.AddColumnOnce(userpasswordpolicy.FieldTenantID)
		}
	}
	if ps := uppq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := uppq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := uppq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := uppq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (uppq *UserPasswordPolicyQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(uppq.driver.Dialect())
	t1 := builder.Table(userpasswordpolicy.Table)
	columns := uppq.ctx.Fields
	if len(columns) == 0 {
		columns = userpasswordpolicy.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if uppq.sql != nil {
		selector = uppq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if uppq.ctx.Unique != nil && *uppq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range uppq.predicates {
		p(selector)
	}
	for _, p := range uppq.order {
		p(selector)
	}
	if offset := uppq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := uppq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UserPasswordPolicyGroupBy is the group-by builder for UserPasswordPolicy entities.
type UserPasswordPolicyGroupBy struct {
	selector
	build *UserPasswordPolicyQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (uppgb *UserPasswordPolicyGroupBy) Aggregate(fns ...AggregateFunc) *UserPasswordPolicyGroupBy {
	uppgb.fns = append(uppgb.fns, fns...)
	return uppgb
}

// Scan applies the selector query and scans the result into the given value.
func (uppgb *UserPasswordPolicyGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, uppgb.build.ctx, ent.OpQueryGroupBy)
	if err := uppgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserPasswordPolicyQuery, *UserPasswordPolicyGroupBy](ctx, uppgb.build, uppgb, uppgb.build.inters, v)
}

func (uppgb *UserPasswordPolicyGroupBy) sqlScan(ctx context.Context, root *UserPasswordPolicyQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(uppgb.fns))
	for _, fn := range uppgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*uppgb.flds)+len(uppgb.fns))
		for _, f := range *uppgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*uppgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := uppgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UserPasswordPolicySelect is the builder for selecting fields of UserPasswordPolicy entities.
type UserPasswordPolicySelect struct {
	*UserPasswordPolicyQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (upps *UserPasswordPolicySelect) Aggregate(fns ...AggregateFunc) *UserPasswordPolicySelect {
	upps.fns = append(upps.fns, fns...)
	return upps
}

// Scan applies the selector query and scans the result into the given value.
func (upps *UserPasswordPolicySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, upps.ctx, ent.OpQuerySelect)
	if err := upps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserPasswordPolicyQuery, *UserPasswordPolicySelect](ctx, upps.UserPasswordPolicyQuery, upps, upps.inters, v)
}

func (upps *UserPasswordPolicySelect) sqlScan(ctx context.Context, root *UserPasswordPolicyQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(upps.fns))
	for _, fn := range upps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*upps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := upps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
